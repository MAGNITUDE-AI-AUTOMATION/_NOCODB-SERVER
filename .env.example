# Environment variables for NocoDB
# Copy this file to .env and update the passwords

# PostgreSQL password (use a strong password)
POSTGRES_PASSWORD=ChangeThisStrongPassword123!

# NocoDB JWT secret (generate with: openssl rand -base64 32)
NC_AUTH_JWT_SECRET=GenerateASecureRandomString456!
